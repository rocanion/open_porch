.tabbed
  = render :partial => 'layouts/admin/nav'
  .clear.whitebox.padded
    .left
      %h1 Edit User

      = formatted_form_for @user, :url => [:admin, @user] do |f|
        = render :partial => 'form', :locals => {:f => f}