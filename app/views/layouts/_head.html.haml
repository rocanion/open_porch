%title Open Porch
= stylesheet_link_tag :all
= javascript_include_tag 'jquery'
= csrf_meta_tag

- if (content_for?(:doc_ready))
  :javascript
    $(document).ready(function() {
      #{yield(:doc_ready)}
    });
    
- if (content_for?(:js))
  :javascript
    #{yield(:js)}
    
= yield(:head)
